
const data=window.COUNCIL_DATA;
const fmt=new Intl.NumberFormat('en-US');
const pct=new Intl.NumberFormat('en-US',{style:'percent',maximumFractionDigits:1});
const one=new Intl.NumberFormat('en-US',{maximumFractionDigits:1,minimumFractionDigits:1});
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function n(v){return v==null||Number.isNaN(v)?'n/a':fmt.format(v)}
function p(v){return v==null||Number.isNaN(v)?'n/a':pct.format(v)}
function sp(v){return v==null||Number.isNaN(v)?'n/a':new Intl.NumberFormat('en-US',{style:'percent',maximumFractionDigits:1,signDisplay:'always'}).format(v)}
function m(v){return v==null||Number.isNaN(v)?'n/a':one.format(v)}
function cls(status){return status==='On Track'?'good':status==='Monitor'?'warn':'bad'}
function rows(){const q=(document.getElementById('search')?.value||'').toLowerCase();return data.districts.filter(r=>!q||[r.district,r.district_chair,r.district_commissioner,r.field_exec].join(' ').toLowerCase().includes(q));}
function table(headers,rows){return `<div class="table-wrap"><table><thead><tr>${headers.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${rows.join('')}</tbody></table></div>`}
function renderKpis(){const c=data.council;const tiles=[['Youth',n(c.members),`${n(c.yoy_delta)} YoY (${sp(c.yoy_pct)})`,(c.yoy_delta||0)>=0?'good':'warning'],['Units',n(c.units),`${n(c.at_risk_units)} at-risk units`,'danger'],['Avg Metric',m(c.avg_metric),`${p(c.healthy_rate)} at metric 4-5`,'teal'],['SYT',p(c.syt_pct),'Safeguarding Youth Training','teal'],['Training',p(c.training_pct),'All scouter training','warning'],['Commissioners',n(c.commissioners),'Unique people','good']];document.getElementById('kpis').innerHTML=tiles.map(t=>`<article class="kpi ${esc(t[3])}"><div><div class="kpi-label">${esc(t[0])}</div><div class="kpi-value">${esc(t[1])}</div></div><div class="kpi-sub">${esc(t[2])}</div></article>`).join('')}
function renderChart(){const max=Math.max(...rows().map(r=>r.members||0),1);document.getElementById('districtChart').innerHTML=rows().map(r=>`<div class="chart-row"><strong>${esc(r.district)}</strong><div class="bar"><span style="width:${Math.max(3,((r.members||0)/max)*100)}%"></span></div><span>${n(r.members)}</span></div>`).join('')}
function renderSignals(){const items=[...data.districts].sort((a,b)=>(b.at_risk_rate||0)-(a.at_risk_rate||0)).slice(0,5);document.getElementById('signals').innerHTML=items.map(r=>`<div class="signal"><strong>${esc(r.district)}</strong><br><span class="${cls(r.status)}">${esc(r.status)}</span> · ${p(r.at_risk_rate)} at risk · ${p(r.training_pct)} trained</div>`).join('')}
function renderDistricts(){document.getElementById('districtRows').innerHTML=rows().map(r=>`<tr><td>${esc(r.district)}</td><td><span class="status ${cls(r.status)}">${esc(r.status)}</span></td><td class="num">${n(r.members)}</td><td class="num">${sp(r.yoy_pct)}</td><td class="num">${p(r.syt_pct)}</td><td class="num">${m(r.avg_metric)}</td><td class="num">${p(r.at_risk_rate)}</td><td class="num">${p(r.training_pct)}</td></tr>`).join('')}
function renderPriority(){document.getElementById('priorityCount').textContent=`${n((data.priority_units||[]).length)} units`;document.getElementById('priorityRows').innerHTML=(data.priority_units||[]).map(r=>`<tr><td>${esc(r.district)}</td><td>${esc(r.unit)}</td><td class="num">${m(r.metric)}</td><td class="num">${n(r.youth)}</td><td>${r.ul_cc_trained?'Yes':'No'}</td><td>${r.small_unit_threshold?'Yes':'No'}</td><td>${r.yoy_growth?'Yes':'No'}</td></tr>`).join('')}
function renderCommissioners(){document.getElementById('commissionerRows').innerHTML=(data.commissioners||[]).slice(0,80).map(r=>`<tr><td>${esc(r.district)}</td><td>${esc(r.name)}</td><td>${esc(r.position)}</td><td>${r.trained?'Yes':'No'}</td><td>${esc(r.assigned_units)}</td></tr>`).join('')}
function renderUnitList(title,items){if(!items?.length)return'';return `<article class="mini-panel"><h3>${esc(title)}</h3>${table(['District','Unit'],items.slice(0,40).map(r=>`<tr><td>${esc(r.district)}</td><td>${esc(r.unit)}</td></tr>`))}</article>`}
function renderTraining(){const host=document.getElementById('trainingContent');const t=data.training||{};const status=document.getElementById('trainingStatus');if(!t.available){status.textContent='No training tab';host.innerHTML='<p class="muted">No training data was found in this workbook.</p>';return}const dive=t.dive||{};const people=t.people||{};const syt=t.syt||{};status.textContent=people.available?`${p(people.trained_pct)} trained`:'Available';const cards=[];if(people.available){cards.push(['Training Records',n(people.total),`${p(people.trained_pct)} trained`,'teal']);cards.push(['Direct Contact',n(people.direct_contact),`${p(people.direct_contact_trained_pct)} trained`,'warning'])}if(syt.available){cards.push(['SYT Exceptions',n(syt.exceptions),`${n(syt.expired)} expired · ${n(syt.due_30)} due 0-30`,'danger'])}if(dive.available&&dive.council){cards.push(['All Scouters',p(dive.council.all_scouters_pct),'Training Dive','teal']);cards.push(['Direct Contact',p(dive.council.direct_contact_pct),'Training Dive','warning'])}let html=`<div class="training-kpis">${cards.map(c=>`<article class="kpi ${esc(c[3])}"><div><div class="kpi-label">${esc(c[0])}</div><div class="kpi-value">${esc(c[1])}</div></div><div class="kpi-sub">${esc(c[2])}</div></article>`).join('')}</div>`;if(dive.districts?.length){html+=`<article class="mini-panel"><h3>District Training Rates</h3>${table(['District','All Scouters','Direct Contact','All Scouters #','Direct Contact #'],dive.districts.map(r=>`<tr><td>${esc(r.district)}</td><td class="num">${p(r.all_scouters_pct)}</td><td class="num">${p(r.direct_contact_pct)}</td><td class="num">${n(r.all_scouters_count)}</td><td class="num">${n(r.direct_contact_count)}</td></tr>`))}</article>`}html+='<div class="training-grid">';html+=renderUnitList('100% Direct Contact Trained Units',dive.direct_contact_honor_roll);html+=renderUnitList('100% All Scouters Trained Units',dive.all_scouters_honor_roll);html+=renderUnitList('Packs Without BALOO Trained Leaders',dive.no_baloo_packs);html+='</div>';if(people.untrained?.length){html+=`<article class="mini-panel"><h3>Untrained Position Records</h3>${table(['District','Unit','Name','Position'],people.untrained.slice(0,50).map(r=>`<tr><td>${esc(r.district)}</td><td>${esc(r.unit)}</td><td>${esc(r.name)}</td><td>${esc(r.position)}</td></tr>`))}</article>`}if(syt.exceptions_sample?.length){html+=`<article class="mini-panel"><h3>SYT Exceptions</h3>${table(['District','Unit','Name','Position','Expires'],syt.exceptions_sample.slice(0,50).map(r=>`<tr><td>${esc(r.district)}</td><td>${esc(r.unit)}</td><td>${esc(r.name)}</td><td>${esc(r.position)}</td><td>${esc(r.expires)}</td></tr>`))}</article>`}host.innerHTML=html}
function renderAll(){renderKpis();renderChart();renderSignals();renderDistricts();renderTraining();renderPriority();renderCommissioners()}
document.getElementById('search')?.addEventListener('input',renderAll);
renderAll();
